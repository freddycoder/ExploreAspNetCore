apiVersion: v1
kind: Service
metadata:
  name: swaggerdoc-apis-service
  labels:
    run: swaggerdoc
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: swaggerdoc